import { Component, ViewChild } from '@angular/core';
import { IonTabs } from '@ionic/angular';

@Component({
  selector: 'app-home',
  templateUrl: 'host.page.html',
  styleUrls: ['host.page.scss'],
})
export class HostPage {
  // @ViewChild('iontabs', { read: IonTabs }) tabRef: IonTabs;

  constructor() {}
}
